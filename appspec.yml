version: 0.0
os: linux
files:
  - source: .
    destination: /home/ubuntu/ira
hooks:
  AfterInstall:
   - location: setup.sh
     timeout: 3600
     runas: ubuntu
  ApplicationStart:
   - location: run.sh
     timeout: 3600
     runas: ubuntu